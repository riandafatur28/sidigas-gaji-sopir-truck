@startuml
!theme plain
skinparam backgroundColor #ffffff
skinparam defaultFontSize 11
skinparam defaultFontName Arial

|Admin|
start
:Buka /dashboard;

|Sistem|
:Query periode aktif\n(syncActiveStatus);
:Query total ritase\n(periode aktif);
:Query total gaji\n(periode aktif);
:Query jumlah sopir & tujuan aktif;
:Query statistik per sopir\n(total rit, gaji, DT);
:Rendering data\nke card & chart;

|Admin|
:Lihat ringkasan dashboard\n(total rit, gaji, sopir\nterbaik, chart);
stop

@enduml
